cmake_minimum_required(VERSION 3.10)
project(cuda_opencv)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(cuda_opencv main.cpp)
target_link_libraries(cuda_opencv ${OpenCV_LIBS})
